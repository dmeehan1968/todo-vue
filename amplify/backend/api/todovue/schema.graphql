type Todo @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  description: String
  completed: Boolean
}

type Mutation {
    toggleCompleted: String @function(name: "toggleCompleted-${env}")
}